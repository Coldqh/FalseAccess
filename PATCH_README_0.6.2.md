# FALSE ACCESS 0.6.2

Распаковать содержимое архива поверх версии 0.6.1 с заменой файлов.

Проверка:

```powershell
cd C:\FalseAccess
npm ci --no-audit --no-fund
npm run build
```

Глава REDTABLE-02 появляется после завершения NORTHLINE-07 и исчезает после закрытия дела.
