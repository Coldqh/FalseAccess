# FALSE ACCESS 0.6.1

Распаковать содержимое архива поверх версии 0.6.0 с заменой файлов.

Проверка:

```powershell
cd C:\FalseAccess
npm ci --no-audit --no-fund
npm run build
```

Глава NORTHLINE-07 появляется после завершения MARSHRUT-01 и исчезает после закрытия дела.
