# PATCH 0.8.1

Распаковать архив поверх FALSE ACCESS 0.8.0 с заменой файлов.

Патч содержит только новые и изменённые файлы. Сохранения мигрируют автоматически. После установки удалить `node_modules` и `dist`, затем выполнить `npm ci` и `npm run build`.
